---
AWSTemplateFormatVersion: '2010-09-09'
Description: Pipeline using CodePipeline and CodeBuild for continuous delivery of a single-page application to S3
Parameters:
  RootDomainName:
    Type: String
    Description: Domain name for your website (example.com)
  GitHubUser:
    Type: String
    Description: GitHub User
    Default: "ethanbergstrom"
  SiteGitHubRepo:
    Type: String
    Description: Site GitHub Repo to pull from. Only the Name. not the URL
    Default: "website"
  SiteGitHubBranch:
    Type: String
    Description: Site GitHub Branch
    Default: "master"
  EnviroGitHubRepo:
    Type: String
    Description: Enviro GitHub Repo to pull from. Only the Name. not the URL
    Default: "enviro"
  EnviroGitHubBranch:
    Type: String
    Description: Enviro GitHub Branch
    Default: "master"
  GitHubToken:
    NoEcho: true
    Type: String
    Description: Secret. It might look something like 9b189a1654643522561f7b3ebd44a1531a4287af OAuthToken with access to Repo. Go to https://github.com/settings/tokens
  BuildType:
    Type: String
    Default: "LINUX_CONTAINER"
    Description: The build container type to use for building the app
  BuildComputeType:
    Type: String
    Default: "BUILD_GENERAL1_SMALL"
    Description: The build compute type to use for building the app
  BuildImage:
    Type: String
    Default: "aws/codebuild/ubuntu-base:14.04"
    Description: The build image to use for building the app
Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label:
          default: "Site Configuration"
        Parameters:
          - RootDomainName
      - Label:
          default: "GitHub Configuration"
        Parameters:
          - GitHubToken
          - GitHubUser
          - SiteGitHubRepo
          - SiteGitHubBranch
          - EnviroGitHubRepo
          - EnviroGitHubBranch
      - Label:
          default: "Build Configuration"
        Parameters:
          - BuildType
          - BuildComputeType
          - BuildImage
    ParameterLabels:
      RootDomainName:
        default: Domain name for your website (example.com)
      GitHubToken:
        default: GitHub OAuth2 Token
      GitHubUser: 
        default: GitHub User/Org Name
      SiteGitHubRepo: 
        default: Site GitHub Repository Name
      SiteGitHubBranch: 
        default: Site GitHub Branch Name
      EnviroGitHubRepo: 
        default: Enviro GitHub Repository Name
      EnviroGitHubBranch: 
        default: Enviro GitHub Branch Name
      BuildType: 
        default: CodeBuild type
      BuildComputeType: 
        default: CodeBuild instance type
      BuildImage: 
        default: CodeBuild image
Resources:
  CodeBuildRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Statement:
        - Effect: Allow
          Principal:
            Service:
            - codebuild.amazonaws.com
          Action:
          - sts:AssumeRole
      Path: "/"
      Policies:
      - PolicyName: codebuild-service
        PolicyDocument:
          Statement:
          - Effect: Allow
            Action: "*"
            Resource: "*"
          Version: '2012-10-17'
  CodePipelineRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Statement:
        - Effect: Allow
          Principal:
            Service:
            - codepipeline.amazonaws.com
          Action:
          - sts:AssumeRole
      Path: "/"
      Policies:
      - PolicyName: codepipeline-service
        PolicyDocument:
          Statement:
          - Action:
            - codebuild:*
            Resource: "*"
            Effect: Allow
          - Action:
            - s3:GetObject
            - s3:GetObjectVersion
            - s3:GetBucketVersioning
            Resource: "*"
            Effect: Allow
          - Action:
            - s3:PutObject
            Resource:
            - arn:aws:s3:::codepipeline*
            Effect: Allow
          - Action:
            - s3:*
            - cloudformation:*
            - iam:PassRole
            Resource: "*"
            Effect: Allow
          Version: '2012-10-17'
  SiteBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Delete
    Properties:
      BucketName: !Ref RootDomainName
      AccessControl: PublicRead
      WebsiteConfiguration:
        IndexDocument: index.html
  WWWBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub www.${RootDomainName}
      AccessControl: BucketOwnerFullControl
      WebsiteConfiguration:
        RedirectAllRequestsTo:
          HostName: !Ref SiteBucket
  DNSZone:
    Type: AWS::Route53::HostedZone
    Properties:
      Name: !Ref RootDomainName
  ApexRecordSet:
    Type: AWS::Route53::RecordSet
    Properties:
      HosteZoneName: !Ref RootDomainName
      Comment: Zone apex
  WWWRecordSet:
    Type: AWS::Route53::RecordSet
    Properties:
      Name: !Sub "www.${RootDomainName}"
      Type: CNAME
      ResourceRecords:
      - !GetAtt WWWBucket.DomainName        
  EnviroBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Delete
  PipelineBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Delete
  CodeBuildDeploySite:
    Type: AWS::CodeBuild::Project
    DependsOn: CodeBuildRole
    Properties:
      Name: !Sub ${AWS::StackName}-DeploySite
      Description: Deploy site to S3
      ServiceRole: !GetAtt CodeBuildRole.Arn
      Artifacts:
        Type: CODEPIPELINE
      Environment:
        Type: !Ref BuildType
        ComputeType: !Ref BuildComputeType
        Image: !Sub ${BuildImage}
      Source:
        Type: CODEPIPELINE
        BuildSpec: !Sub |
          version: 0.1
          phases:
            post_build:
              commands:
                - aws s3 cp --recursive --acl public-read ./ s3://${RootDomainName}/ 
      TimeoutInMinutes: 10
  CodeBuildEnviroSandbox:
    Type: AWS::CodeBuild::Project
    DependsOn: CodeBuildRole
    Properties:
      Name: !Sub ${AWS::StackName}-EnviroSandbox
      Description: Deploy Enviro to Sandbox
      ServiceRole: !GetAtt CodeBuildRole.Arn
      Artifacts:
        Type: CODEPIPELINE
      Environment:
        Type: !Ref BuildType
        ComputeType: !Ref BuildComputeType
        Image: !Sub ${BuildImage}
        EnvironmentVariables:
          - SDB_DOMAIN: enviroSandbox
          - SDB_REGION: !Ref "AWS::Region"
      Source:
        Type: CODEPIPELINE
        BuildSpec: !Sub |
          version: 0.2
          phases:
            post_build:
              commands:
                - sam package --template-file template.yaml --s3-bucket ${EnviroBucket.Arn} --output-tempate-file packaged.yaml
                - sam deploy --template-file packaged.yaml --stack-name ${AWS::StackName} --capabilities CAPABILITY_IAM
      TimeoutInMinutes: 10
  Pipeline:
    Type: AWS::CodePipeline::Pipeline
    Properties:
      RoleArn: !GetAtt CodePipelineRole.Arn
      Stages:
      - Name: Source
        Actions:
        - InputArtifacts: []
          Name: Site Source
          ActionTypeId:
            Category: Source
            Owner: ThirdParty
            Version: '1'
            Provider: GitHub
          OutputArtifacts:
          - Name: SiteSourceOutput
          Configuration:
            Owner: !Ref GitHubUser
            Repo: !Ref SiteGitHubRepo
            Branch: !Ref SiteGitHubBranch
            OAuthToken: !Ref GitHubToken
          RunOrder: 1
        - InputArtifacts: []
          Name: Enviro Source
          ActionTypeId:
            Category: Source
            Owner: ThirdParty
            Version: '1'
            Provider: GitHub
          OutputArtifacts:
          - Name: EnviroSourceOutput
          Configuration:
            Owner: !Ref GitHubUser
            Repo: !Ref EnviroGitHubRepo
            Branch: !Ref EnviroGitHubBranch
            OAuthToken: !Ref GitHubToken
          RunOrder: 2
      - Name: Deploy
        Actions:
        - Name: SiteArtifact
          ActionTypeId:
            Category: Build
            Owner: AWS
            Version: '1'
            Provider: CodeBuild
          InputArtifacts:
          - Name: SiteSourceOutput
          OutputArtifacts:
          - Name: SiteDeployOutput
          Configuration:
            ProjectName: !Ref CodeBuildDeploySite
          RunOrder: 1
        - Name: EnviroArtifact
          ActionTypeId:
            Category: Build
            Owner: AWS
            Version: '1'
            Provider: CodeBuild
          InputArtifacts:
          - Name: EnviroSourceOutput
          OutputArtifacts:
          - Name: EnviroDeployOutput
          Configuration:
            ProjectName: !Ref CodeBuildEnviroSandbox
          RunOrder: 2
      ArtifactStore:
        Type: S3
        Location: !Ref PipelineBucket
Outputs:
  PipelineUrl:
    Value: !Sub https://console.aws.amazon.com/codepipeline/home?region=${AWS::Region}#/view/${Pipeline}
    Description: CodePipeline URL
  SiteUrl:
    Value: !GetAtt [SiteBucket, WebsiteURL]
    Description: S3 Website URL
